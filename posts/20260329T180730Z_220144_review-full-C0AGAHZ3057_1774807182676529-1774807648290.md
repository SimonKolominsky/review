*ВЕРДИКТ*
Предварительно: блокирующих дефектов по измененному диффу не найдено, но ревью неполное, так как выполнено одним воркером (без обязательного мульти-модельного fanout).

*КЛЮЧЕВЫЕ НАХОДКИ*
- Вынесение `migrations` и языковых методов из `AbstractLabel` в `WhiteLabel` выглядит корректным и семантически точным: функциональность теперь доступна только для нужного подтипа.
- В `UserPlanListBuilder` добавлен безопасный fallback для не-`WhiteLabel` владельца метки: вместо потенциального вызова WL-методов теперь возвращается перевод плана.
- В `UserPlanFunnelStepMatcher` добавлена явная проверка типа и контролируемое исключение с понятным сообщением, плюс покрытие тестом негативного сценария.
- Добавлен unit-тест для fallback-пути в `UserPlanListBuilder`, что снижает риск регрессии в этой ветке.

*РИСКИ*
- Процессный риск: нет мульти-модельной агрегации ревью (reports_count < 2), поэтому уровень уверенности ниже стандартного для пайплайна review.

*ПРОВЕРКИ ПЕРЕД MERGE*
- Прогнать full CI для ветки PR (phpunit + статанализаторы проекта).
- Проверить smoke-сценарий WL-пользователя и не-WL пользователя на экранах/эндпоинтах, где используется подбор плана.

*КРАТКИЙ ИТОГ*
Кодовые изменения выглядят логичными и направлены на корректное разделение ответственности между `AbstractLabel` и `WhiteLabel`. При наличии зеленого CI и подтверждения вторым ревьюером PR можно принимать.

PR: https://github.com/systemeio/backend/pull/22333
