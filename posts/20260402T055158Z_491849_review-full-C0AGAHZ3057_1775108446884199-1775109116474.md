*ВЕРДИКТ*
- ✅ APPROVE: блокирующих проблем не найдено.

*КЛЮЧЕВЫЕ НАХОДКИ*
- Контроллер `PaymentPageController` успешно переведен с form-type на DTO + `MapRequestPayload`, логика вынесена в `PaymentPageModelBuilder`.
- Доступ ограничен через `#[IsGranted(WorkspaceMemberRoleVoter::IS_WORKSPACE_OWNER)]` для `GET` и `PUT`, что эквивалентно прежней проверке и чище по стилю.
- Добавлен `CODEOWNERS` для `src/Service/User/PaymentPage/**`, замечание из обсуждения PR закрыто.

*РИСКИ*
- Низкий: при неполном payload DTO использует значения по умолчанию, поэтому клиент должен отправлять полный набор полей для `PUT`.

*ПРОВЕРКИ ПЕРЕД MERGE*
- Прогнать API тесты настроек payment page (GET/PUT happy path и частичный payload).
- Проверить сериализацию/десериализацию multilang полей (`paymentPageAgreement`, `salesInvoiceFooter`).

*КРАТКИЙ ИТОГ*
- PR аккуратно удаляет `PaymentPageType`, делает код более явным и поддерживаемым, архитектурно изменение выглядит корректным.
- PR: https://github.com/systemeio/backend/pull/22520
