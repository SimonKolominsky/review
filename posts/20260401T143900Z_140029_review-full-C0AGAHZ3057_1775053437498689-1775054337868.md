*ВЕРДИКТ*
approve_with_notes

*КЛЮЧЕВЫЕ НАХОДКИ*
- Блокирующих проблем в изменениях не найдено.
- В `UserPlanListBuilder` добавлен безопасный fallback на перевод, если `getOwnLabel()` не является `WhiteLabel`.
- В `UserPlanFunnelStepMatcher` добавлена явная проверка типа с `RuntimeException`, плюс unit-тест на этот сценарий.

*РИСКИ*
- Низкий: в случае неконсистентных данных вместо неявной ошибки теперь будет явный `RuntimeException` (это ожидаемо, но может увеличить видимость проблем данных в рантайме).

*ПРОВЕРКИ ПЕРЕД MERGE*
- Прогнать unit-тесты для `UserPlanListBuilderTest` и `UserPlanFunnelStepMatcherTest`.
- Проверить, что CI проходит на ветке PR.

*КРАТКИЙ ИТОГ*
PR выглядит корректно и улучшает устойчивость после рефакторинга иерархии `AbstractLabel`: https://github.com/systemeio/backend/pull/22333
